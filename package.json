{
  "name": "tw2k-tactical",
  "version": "0.5.2-alpha",
  "type": "module",
  "scripts": {
    "build": "vite build",
    "test": "vitest run",
    "test:watch": "vitest",
    "verify": "tsc --noEmit && vitest run && vite build"
  },
  "devDependencies": {
    "typescript": "^5",
    "vite": "^6",
    "vitest": "^4.1.10"
  }
}
